<div class=”fields”> # Engloba todos campos
  <%= form_for([@user, @contact]) do |form| %>
    <% if contact.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

        <ul>
        <% contact.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
      <% end %>

      <%= form.fields_for :phones do |builder| %>
        <%= render "phone_fields", form: builder %>
      <% end %>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
<% end %> # “end” do fields_for
<p><%= link_to_add_fields "Add Phone", f, :phones %></p>
</div>
